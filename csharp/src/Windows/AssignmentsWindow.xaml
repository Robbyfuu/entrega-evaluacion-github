<Window
    x:Class="EntregaEvaluacion.Windows.AssignmentsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Tareas de GitHub Classroom"
    Width="580"
    Height="440"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Background="{DynamicResource SurfaceBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Tareas asignadas por el profesor"
            Style="{StaticResource H2Text}"
            FontSize="18"
            Margin="0,0,0,8" />

        <TextBlock
            Grid.Row="1"
            Text="Click 'Aceptar' -> se abre Classroom -> acepta en GitHub -> vuelve y usa 'Repositorio existente'."
            Style="{StaticResource MutedText}"
            TextWrapping="Wrap"
            Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ItemsHost">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                Text="{Binding Title}"
                                Style="{StaticResource LabelText}"
                                FontWeight="Bold"
                                VerticalAlignment="Center"
                                TextWrapping="Wrap" />
                            <ui:Button
                                Grid.Column="1"
                                Content="Aceptar tarea"
                                Appearance="Success"
                                Width="150"
                                Tag="{Binding ClassroomUrl}"
                                Click="Accept_Click" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <ui:Button
            Grid.Row="3"
            Content="Cerrar"
            Width="120"
            Height="36"
            HorizontalAlignment="Center"
            Margin="0,12,0,0"
            Click="Close_Click" />
    </Grid>
</Window>
