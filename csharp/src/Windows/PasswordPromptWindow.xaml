<Window
    x:Class="EntregaEvaluacion.Windows.PasswordPromptWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Clave del profesor"
    Width="420"
    Height="230"
    WindowStartupLocation="CenterOwner"
    WindowStyle="SingleBorderWindow"
    ResizeMode="NoResize"
    Topmost="True"
    Background="{DynamicResource SurfaceBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Ingrese la clave de desbloqueo:"
            Style="{StaticResource H2Text}"
            Margin="0,0,0,12" />

        <PasswordBox
            x:Name="PwdBox"
            Grid.Row="1"
            FontFamily="{DynamicResource MonoFont}"
            FontSize="16"
            Padding="6"
            Margin="0,0,0,8"
            KeyDown="PwdBox_KeyDown" />

        <TextBlock
            x:Name="ErrText"
            Grid.Row="2"
            Foreground="{DynamicResource DangerBrush}"
            FontWeight="Bold"
            Margin="0,0,0,8" />

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom">
            <Button
                Content="Validar"
                Width="100"
                Height="36"
                Margin="0,0,8,0"
                Foreground="White"
                Background="{DynamicResource SuccessBrush}"
                BorderThickness="0"
                Click="Ok_Click" />
            <Button
                Content="Cancelar"
                Width="100"
                Height="36"
                Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
