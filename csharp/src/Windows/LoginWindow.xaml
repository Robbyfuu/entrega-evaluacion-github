<Window
    x:Class="EntregaEvaluacion.Windows.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Iniciar sesion en GitHub"
    Width="580"
    Height="500"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Background="{DynamicResource SurfaceBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Paso 1 -->
        <TextBlock
            Grid.Row="0"
            Text="PASO 1: Abre esta URL en tu navegador o celular"
            Style="{StaticResource H2Text}"
            Margin="0,0,0,8" />

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox
                x:Name="UrlBox"
                Grid.Column="0"
                IsReadOnly="True"
                FontFamily="{DynamicResource MonoFont}"
                Text="https://github.com/login/device"
                Margin="0,0,8,0" />
            <ui:Button
                x:Name="CopyUrlButton"
                Grid.Column="1"
                Content="Copiar"
                Width="110"
                Click="CopyUrl_Click" />
        </Grid>

        <!-- Paso 2 -->
        <TextBlock
            Grid.Row="2"
            Text="PASO 2: Ingresa este codigo"
            Style="{StaticResource H2Text}"
            Margin="0,0,0,8" />

        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border
                Grid.Column="0"
                Background="#212121"
                CornerRadius="8"
                Margin="0,0,8,0">
                <TextBlock
                    x:Name="CodeText"
                    Text="....-...."
                    FontFamily="{DynamicResource MonoFont}"
                    FontSize="36"
                    FontWeight="Bold"
                    Foreground="#4ADE80"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Padding="0,10" />
            </Border>
            <ui:Button
                x:Name="CopyCodeButton"
                Grid.Column="1"
                Content="Copiar codigo"
                Width="110"
                VerticalAlignment="Center"
                Click="CopyCode_Click" />
        </Grid>

        <ui:Button
            x:Name="OpenButton"
            Grid.Row="4"
            Content="Abrir URL en navegador (opcional)"
            HorizontalAlignment="Stretch"
            Margin="0,0,0,16"
            Click="Open_Click" />

        <TextBlock
            x:Name="StatusText"
            Grid.Row="5"
            Text="Solicitando codigo..."
            FontStyle="Italic"
            Foreground="{DynamicResource WarningBrush}"
            HorizontalAlignment="Center"
            Margin="0,0,0,8" />

        <ProgressBar
            x:Name="Progress"
            Grid.Row="6"
            IsIndeterminate="True"
            Height="6"
            Margin="0,0,0,8" />

        <TextBlock
            x:Name="TimeText"
            Grid.Row="7"
            Foreground="{DynamicResource FaintBrush}"
            FontSize="12"
            HorizontalAlignment="Center" />

        <Button
            Grid.Row="9"
            Content="Cancelar"
            Width="120"
            Height="36"
            HorizontalAlignment="Center"
            Margin="0,8,0,0"
            Click="Cancel_Click" />
    </Grid>
</Window>
