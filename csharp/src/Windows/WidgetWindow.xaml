<Window
    x:Class="EntregaEvaluacion.Windows.WidgetWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Tiempo restante"
    Width="224"
    Height="120"
    WindowStyle="None"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Topmost="True"
    AllowsTransparency="True"
    Background="Transparent">

    <!-- Card flotante (look Fluent de la app). AllowsTransparency + Background
         Transparent dejan ver las esquinas redondeadas del Border. -->
    <Border
        Background="{DynamicResource SurfaceBrush}"
        BorderBrush="{DynamicResource BorderStrongBrush}"
        BorderThickness="1"
        CornerRadius="10"
        Padding="14,10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.22" Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Text="TIEMPO RESTANTE"
                FontFamily="{DynamicResource UiFont}"
                FontSize="10"
                FontWeight="SemiBold"
                Foreground="{DynamicResource MutedBrush}" />

            <TextBlock
                x:Name="TimeText"
                Grid.Row="1"
                Text="--:--:--"
                FontFamily="{DynamicResource MonoFont}"
                FontSize="28"
                FontWeight="Bold"
                Foreground="{DynamicResource TextBrush}"
                Margin="0,2,0,8" />

            <ui:Button
                x:Name="GoButton"
                Grid.Row="2"
                Content="Ir a la entrega"
                Appearance="Primary"
                HorizontalAlignment="Stretch"
                Click="GoButton_Click" />
        </Grid>
    </Border>
</Window>
